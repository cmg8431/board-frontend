import styled from '@emotion/styled'

export const Title = styled.div`
  font-size: 2.5rem;
  font-weight: 600;
  letter-spacing: -0.1rem;
`

export const Cotnent = styled.div`
  font-size: 1.5rem;
  font-weight: 400;
  letter-spacing: -0.1rem;

  border-top: 1px solid var(--color-garyblue-100);
  border-bottom: 1px solid var(--color-garyblue-100);
`

export const ContentPre = styled.pre`
  & > p {
    font-size: 1.8rem;
  }
`

export const SubTitle = styled.div`
  font-size: 1.6rem;
  margin-right: 1rem;
`

export const SubTitleContainer = styled.div`
  display: flex;
  margin-bottom: 1.5rem;
`
